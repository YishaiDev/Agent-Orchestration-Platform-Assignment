# Google Gemini (required) — used by all agents via init_chat_model(model_provider="google_genai")
GOOGLE_API_KEY=your-gemini-api-key

# Tavily (required for the Research Agent's web search; not used by the Writing Agent)
TAVILY_API_KEY=your-tavily-api-key
