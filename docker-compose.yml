services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: agent-orchestration-platform
    ports:
      - "8000:8000"
    env_file:
      - app/.env
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    restart: unless-stopped
